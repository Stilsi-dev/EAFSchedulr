## Frontend Source

This folder contains the React + Vite redesign that powers the Flask UI shell.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
  